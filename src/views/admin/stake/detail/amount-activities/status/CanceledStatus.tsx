import type { PoolDetailResponse } from "@/types/pool";

type Props = {
    poolDetail?: PoolDetailResponse;
};

const CanceledStatus = ({ poolDetail: _poolDetail }: Props) => {
    return <></>;
};

export default CanceledStatus;
